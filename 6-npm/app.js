// === NPM ===
//
// perintah :
// `npm init` 						=> harus meng-inisialisasi dlu
// `npm test` 						=> utk test error (script ada di package.json)
// `npm start` 						=> utk menjalankan aplikasi kita (bebas di custom perintahnya)
// `npm i <nama_package>` 			=> menginstall package
// `npm uninstall <nama_package>` 	=> meng-uninstall
// `npm run <script>`				=> menjalankan script selain dr `start` dan `test` 
//
// -- package.json --
// => letak konfigurasi dasar npm 
// -- package-lock.json --
// => letak config detail dependensi yg diinstall
//
// -- npm install dgn versi --
// `npm i <nama_paket>@13.6.0`
// 
console.log('Hello world')

